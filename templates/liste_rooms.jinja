{% extends 'base.jinja' %}
{# recup url du site #}
{# {{ url_for(request.endpoint) }} #}
{% from 'macros/_acceuil.jinja' import room_widget %}

{% block title %}
{{ super() }} - Accueil
{% endblock %}

{% block content %}
<main class="container bg-black pt-3">
    <div class="container-index row">
        <div class="d-flex row align-items-md-stretch">
            {% for r in rooms %}
            {{room_widget(r.name, r.description, r.url_name, 6)}}
            {% endfor %}
        </div>
    </div>
</main>

{% endblock %}